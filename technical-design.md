# 彩色箭头消消乐技术方案

日期：2026-09-09 · 状态：设计方案，尚未实现或实测

配套需求：[prd.md](prd.md)。本文落实已确认的双端路线、抽线规则和可解关卡生成方法；数值参数仍需原型验证。

## 1. 单工程架构

采用 Cocos Creator + TypeScript，只发布独立微信小游戏。日常用 Cocos 自带的浏览器预览调试，微信构建用微信开发者工具和真机验证。不另行开发 H5 Canvas 渲染器。

| 模块 | 职责 | 依赖边界 |
| --- | --- | --- |
| 规则核心 | 几何、阻挡、合法动作、扣心去重、胜负 | 纯 TypeScript，不访问 Cocos、DOM 或 wx |
| 游戏表现 | 线条绘制、抽线动画、界面、输入坐标转换 | 使用 Cocos，读取规则状态 |
| 关卡内容 | 轮廓、路径、颜色、资源与版本 | 两端共用静态数据 |
| 平台适配 | 存档、前后台、震动等 | H5 与微信分别实现，规则层不感知平台 |
| 离线内容工具 | 候选生成、几何校验、求解、质量报告 | 复用规则核心，发布前运行 |

不使用物理引擎，不为两个平台复制玩法代码。H5 与微信的本地存档独立；本阶段不建设账号和云同步。H5 的音频启动、窗口变化，以及微信的安全区和前后台均需分别验收。

Cocos 官方提供 [Web 发布流程](https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/publish-web.html) 和 [微信小游戏发布流程](https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/publish-wechatgame.html)。候选版本为 3.8 系列，验证后锁定具体版本及构建配置。

## 2. 统一几何模型

关卡使用固定逻辑坐标和隐藏正交网格。屏幕尺寸只改变等比例显示和留白，不改变关卡几何、线宽比例或阻挡关系。

每根箭头包含唯一 ID、从尾到头的路径点、与末段一致的朝向、基础颜色。路径不自交、不分叉，与其他箭头保持可见间距。线身采用统一宽度，箭头头部采用确定尺寸；圆角必须包含在约定的几何范围内。

抽线时，头部沿末段方向延伸，尾部沿原路径跟随。移动中的线身占用原路径剩余部分及前方延伸路径。新增空间的碰撞检查覆盖线身与箭头头部的完整退出通道，延伸至形状完全离开棋盘。自身路径不算阻挡，外部装饰不参与判定。

静态路径合法性检查先保证原路径附近没有其他箭头碰撞，因此抽线时只需检测新增通道。尖角、圆角和箭头尺寸采用统一几何定义；边界接触按受阻处理，生成时另留明确间距，避免擦边谜题。

触摸热区可大于可见线条，但不参与障碍碰撞。规则核心接收已命中的箭头 ID；模糊命中由输入层忽略，不能扣心。

## 3. 图案生成流程

视觉依据为用户提供的密集猫咪箭头截图：以正交折线填满剪影，并通过区域配色表现特征，成品使用更明亮丰富的颜色。生成器需要同时处理区域覆盖、局部特征和线间距，不能仅生成稀疏轮廓。早期 PRD 线数仅用于玩法验证，需另做接近参考密度的样例评估绘制与命中性能。

### 3.1 轮廓与美术约束

人工制作猫、狗、云朵等简单矢量轮廓，将其转换为可放置网格区域。约束覆盖线宽后的完整形状，不能只检查路径中心点是否在轮廓内。

标记耳朵、尾巴、云边等关键区域、必要留白、允许的路径长度和转弯数。关键特征可由人工提出候选路径，但不能跳过可解性检查。轮廓是初始布置区域，不是退出时的墙壁。

配色在路径生成后按语义区域分配，保证耳朵、面部等特征可辨识，并考虑相邻箭头色差。一根箭头仍使用一种基础色；若路径跨越多个特征区影响辨识，调整路径后重新求解，不能只靠颜色掩盖问题。

密集布局采用全图观察、放大操作的视图。输入层统一将缩放和平移后的屏幕坐标逆变换为棋盘坐标；双指手势、超过拖动阈值的输入及缩放控制不产生点击。恢复全图仅重置视图。H5 鼠标与触屏、微信触屏分别验收，不能把显示缩放用于修改规则几何。

### 3.2 反向构造

从空棋盘或已验算可解的初始布局开始。反复提出新箭头候选，每个候选需满足：

1. 路径合法，符合轮廓与特征约束。
2. 初始线身与头部不与现有箭头交叠，间距达标。
3. 新箭头在加入后的棋盘上可以立即退出。
4. 加入后布局的密度和形态仍有继续制作的空间。

新箭头可以阻挡旧箭头。不能要求所有旧箭头继续保持可移动，否则会不必要地削弱阻挡层次。

保存加入顺序作为构造记录。候选放不下时，进行有上限的重试、回退或重新生成；仍达不到质量要求则拒绝该候选或调整轮廓/密度。记录随机种子、生成器版本和规则版本，确保问题可复现。

### 3.3 可解性证明

设已有棋盘 B 可解，新加入箭头 a 在 B 加 a 的状态下能退出。先移除 a 后恢复为 B，因此新棋盘仍可解。空棋盘可解，由此逐次添加获得的棋盘可解。

例如加入顺序为 A、B、C，则存在消除顺序 C、B、A。对于可解初始布局，先逆序清除新增箭头，再执行初始布局的解序列。

该证明保证满足构造条件的结果可解，不保证搜索一定能填满任意轮廓，也不保证结果有趣、美观或达到目标难度。

### 3.4 最终独立验算

从最终序列化数据重新加载关卡，重新计算全部阻挡关系，不信任生成器缓存或构造记录。这里“独立”指重新验算，几何规则仍复用同一个规则核心。

不断寻找并移除当前合法箭头，直到全部清空或无法继续。全部清空才能通过；保存完整解序列，并用状态转换流程回放核对。任何人工调整路径、方向、线宽或箭头尺寸后都必须重新验算。

依赖图中，若 A 的退出通道被 B 挡住，记作 A 依赖 B。固定路径、只能移除箭头的规则下，存在循环依赖则无法清空，无环则可以逐步清空。依赖图可用于诊断和计算难度；实际几何仍是判定依据。

移除只会减少阻挡，所以任何合法选择都不会制造新的死局。误点只改变心数与显示色，不改变几何。这些性质在加入旋转、移动障碍等机制后不再自动成立。

## 4. 质量筛选与发布数据

缩放防误触作为核心交互验收：输入需要区分待点击、拖动与双指缩放。一旦进入拖动或缩放，本轮触摸序列永久取消点击，全部触点释放后才允许新的点选。点击容错距离由屏幕坐标换算到逻辑坐标，不能随画布无上限扩大。缩放与平移只作用于棋盘容器，HUD 保持固定；缩放锚点和边界限制需保证所有局部区域可达。最大倍率根据最密集关卡的实际可点性确定。

可解候选继续按下列指标评估：初始出口数量、最长依赖链、解局中可选箭头数量、单次消除释放的箭头数量、线长与转弯分布、间距、轮廓特征覆盖率。

一条解序列只反映一种操作顺序；选择数等动态指标应注明采样方式，必要时采用多个合法顺序采样，不能将单次结果当作所有玩家体验。图案辨识和实际难度最终由人工试玩确认。

发布内容包括关卡 ID、关卡版本、规则版本、逻辑边界、几何尺寸、箭头路径和配色、主题资源引用。生成种子、质量报告、解序列主要保留在制作与验证产物中。运行时提示根据当前棋盘找合法箭头，不强迫玩家遵循保存的解序列。

游戏加载经过验证的静态关卡，不在玩家开局时随机搜索布局。首批先完成云、猫、狗三关，再按 PRD 扩充。

## 5. 状态提交与恢复

一次合法消除先提交逻辑状态和存档，然后播放离场动画；动画期间锁定棋盘。视觉移除完成后，若逻辑棋盘为空，显示通关结果。恢复时直接显示已提交的状态，不重复扣心或补做动作。规则核心与动画完成回调不能各自消除一次。

一次首次误点同时提交心数减少及 penalizedIds 更新；红色由集合推导。重复误点保持集合与心数不变。挑战重开生成新 attemptId，恢复初始状态。

关卡加载时校验版本和基础结构。存档与关卡不兼容时按 PRD 重开本关并保留解锁进度。性能优化不能改变几何结果或状态提交顺序。

## 6. 后续验证清单

- 构造性质：每次新增箭头立即可退出，最终逆序确实能清空。
- 求解诊断：人为制作循环阻挡关卡应被拒绝，空出口不等于整局无解。
- 几何边界：远处阻挡、箭头头部、线宽、圆角及擦边案例。
- 单调性质：任意合法消除后，不增加其他箭头的阻挡集合。
- 状态一致性：重复点击、动画中断、首次误点后重启不重复扣心。
- 双端一致性：相同关卡与相同箭头操作序列产生相同规则结果。
- 视觉与交互：猫狗云可辨认，热区不造成误扣，目标密度下抽线无穿透。

验证完成后再锁定引擎补丁版本、线宽、头部尺寸、网格间距和动画时长。当前仅记录方案，不代表代码、关卡或这些检查已经完成。
