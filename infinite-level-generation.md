# 《一箭清空》参数化无限关卡生成方案

版本：v0.4

阶段：参数化试玩 demo 已实现，完整方案分阶段落地

## 1. 当前状态

试玩版现已由 `assets/scripts/core/Generator.ts` 在运行时生成云朵、小猫、爱心、小兔、小花、小狗。关号决定种子、比例和姿态；关卡按序继续，图案手册支持分页跳转。构造和最终求解共用现有玩法规则。

已完成：六类参数遮罩、箭头铺设、倒序可解构造、四角色配色、关号复现、分帧生成、下一关预生成和分页试玩。试玩版使用独立存档，不覆盖原三关进度。

轮廓修正版使用 `ShapeGrammar.ts`：六类各六种结构构图，共 36 种，包括雨云、月亮枕云、跃起兔、郁金香、双心等。优先改变结构和姿态，细微缩放仅作辅助。所有类别统一与之前 24 关比较归一化剪影，保留宽高比并比较镜像；IoU 达到 0.82 的候选拒绝进入箭头铺设。生成后的箭头占据区域也在批量测试中做相同检查。这是近期相似度保护，不能替代人工美术评审。

当前有限的 36 种构图仍会在后续再次出现，不宣称已实现全历史视觉不重复。尚未完成：更大范围的部件组合语法、全历史近似去重、完整难度调度、综合质量评分、插画奖励和真机性能验收。下文相关部分仍为设计目标，覆盖率和耗时门槛尚未全部达到。

## GitHub 算法参考与迁移结论

已检索并阅读两个与本项目规则接近的开源实现：

- [gtxPrime/arrow-escape](https://github.com/gtxPrime/arrow-escape)：采用“超长、长、中等箭头优先，最后少量补洞”的分阶段生成，并在生成后执行求解和难度验证。
- [nitzanpap/arrout 的生成算法说明](https://github.com/nitzanpap/arrout/blob/main/docs/02-algorithm.md)：明确描述蛇形箭头的反向构造、循环依赖检测、自由度曲线和最小路径约束。

这两个实现验证了当前问题的根因：随机生长后再补短线，不能同时得到密集轮廓、长路径和窄分支。后续第 3 关应改成以下独立流程：

1. 对爱心遮罩做连通区域和走廊分区，先生成长度 6～18、至少两个转角的候选路径。
2. 使用回溯而非贪心占格；每次选择“剩余可用邻居最少”的区域，防止末尾碎成单格。
3. 对候选路径进行反向放置，放置时检查入口轨迹、自身死结和已有依赖。
4. 在每次放置后计算当前自由度，要求目标解序列的大部分阶段只有 1～2 个合法选择。
5. 发现环、短路径、空洞过大或自由度超标时回溯到上一个分区点。
6. 通过独立求解器和人工缩略图检查后，才接受整关。

当前本地 demo 仍使用旧生成器，GitHub 参考方案尚未合入运行时代码。这样可以避免把只有十几根长线或无法求解的实验候选交给试玩。

## 2. 设计目标

- 每一关的整体剪影、箭头布局或二者组合均不重复。
- 图案保持极简、治愈，缩略状态下也能辨认主题。
- 每关一定存在完整解法，不出现箭头自身死结。
- 同一关卡编号在同一生成器版本下稳定复现。
- 不依赖网络，不需要保存无限份关卡 JSON。
- 生成下一关时不阻塞操作，低端微信设备也能流畅运行。

“无限”指参数空间可持续生成，且对玩家已经玩过的关卡进行去重。数学意义上的永久绝不重复无法由有限设备存储证明，因此采用确定性种子、精确结构指纹和近似视觉指纹三层控制。

核心流水线为：

```text
关卡编号 + 生成器版本
  → 可复现 seed 与主题排期
  → 部件语法生成封闭矢量轮廓
  → 栅格化为填充遮罩和语义区域
  → 按可解顺序倒序铺设箭头
  → 抽出路径碰撞与独立求解验证
  → 美观、辨识度、难度综合评分
  → 精确结构与近似视觉去重
  → 输出 LevelRecipe 对应的最终关卡
```

这里生成的是可被箭头填满的封闭剪影，不是只有一条线的轮廓骨架。边缘箭头负责勾勒外形，内部箭头负责保持主体饱满，受控留白只用于眼睛、嘴巴等少量辨识特征。

## 3. 六类图案语法

图案不使用图片素材，由椭圆、胶囊、圆角矩形、折线、多边形、圆弧和布尔运算组合成矢量轮廓，再采样到逻辑网格。

| 类别 | 首批母题 | 主要参数 |
| --- | --- | --- |
| 萌宠 | 猫、狗、兔、熊 | 耳朵形状、头身比、尾巴方向、坐姿、胖瘦 |
| 自然 | 云、月、星、落叶、花瓣 | 云瓣数量、月相、星角数、叶片弯曲、花瓣数量 |
| 软物 | 棉花糖、饼干、泡泡、毛线球 | 圆角、缺口、叠放数量、膨胀程度 |
| 植物 | 小草、多肉、小花、银杏 | 叶片数量、展开角度、枝干弯曲、左右疏密 |
| 风物 | 彩虹、薄雾、雨滴、雪团、太阳 | 层数、弧度、偏移、大小组合 |
| 符号 | 爱心、小房子、礼物、笑脸 | 宽高比、屋顶、门窗、缎带和表情 |

每个母题不是一张固定模板，而是一棵可组合的“部件语法树”。例如猫可以由以下层级组成：

- 主构图：头像、坐姿、趴姿、团卧、探头。
- 结构部件：圆脸或扁圆脸，尖耳或圆耳，长尾或团尾。
- 表情留白：闭眼、豆豆眼、困困眼、腮红区。
- 附加关系：抱星星、趴云朵、戴围巾、挂铃铛、顶小花。
- 连续参数：头身比、胖瘦、耳距、倾斜、曲率、部件偏移和装饰尺度。

狗、兔、云、月亮、蘑菇、茶杯和小房子使用各自独立的部件语法。种子改变主构图、部件组合和连续参数，而不是只对整幅图缩放或平移。生成结果先保留为矢量轮廓及语义区域，随后才栅格化，因此后续能准确计算填充、碰撞和颜色区域。这里的“矢量”是生成器内部的贝塞尔曲线、多边形和布尔轮廓数据，不要求微信运行时解析 SVG 文件；调试工具可以额外导出 SVG 供人工查看。

首版母题应优先选择轮廓辨识度高、内部面积充足的形态。眼睛、嘴巴和铃铛等细节不强制铺设箭头，可用受控留白表现；过细、无法形成清晰箭头路径的装饰在轮廓质检阶段直接舍弃。

## 4. 关卡配方

运行时只需保存紧凑配方：

```ts
type LevelRecipe = {
  levelNumber: number;
  generatorVersion: number;
  seed: number;
  retry: number;
  category: string;
  motif: string;
  difficulty: number;
  paletteIndex: number;
};
```

初始种子由 `hash(游戏固定盐, generatorVersion, levelNumber)` 得到。生成失败或指纹重复时增加 `retry`，重新派生种子。同一个配方始终生成完全相同的关卡。

## 5. 生成流水线

### 5.1 主题排期

类别采用洗牌袋调度：六类全部出现一次后再洗牌，禁止连续两关使用相同类别；同一母题进入至少 12 关冷却。这样既随机，又不会连续出现几只相似的小猫。

### 5.2 参数化轮廓

1. 根据母题和种子生成标准化矢量部件。
2. 使用并集、差集和圆角处理得到封闭轮廓。
3. 采样成逻辑网格遮罩。
4. 清除孤立小块，填补单格孔洞。
5. 检查连通性、最窄通道、面积和外接比例。
6. 计算从边界向内的距离场，以及耳朵、尾巴、花瓣等语义区域。

轮廓质量不达标时直接更换参数，不进入箭头生成阶段。

### 5.3 铺设箭头路径

将遮罩内网格拆分为多条互不重叠的正交自避路径：

1. 优先覆盖边缘和狭窄区域，避免最后留下无法成线的单格。
2. 路径长度随难度变化，短线用于轮廓细节，长线用于主体色块。
3. 每根线至少两个点，转角数和连续直线长度设上下限。
4. 箭头头部出口不得穿过自己的旧线段，提前排除自身死结。
5. 剩余零碎格通过局部回退、合并邻线或重新生长解决。

目标覆盖率不低于 82%，同时限制过大的内部空洞，使剪影更接近完整插画。

### 5.4 按解法倒序构造依赖关系

系统从空盘面开始逐根加入箭头。每次加入的新箭头，在当时已有的盘面中必须能完整飞出：

1. 候选箭头先完成路径、自身死结和轮廓约束检查。
2. 使用与正式游戏相同的抽出路径碰撞函数：头部沿最终方向前进，身体沿已有折线路径逐渐抽出，检查头部出口走廊与其他箭头的线段和头部是否相交。
3. 候选箭头与已有箭头不重叠，并且当前能够飞出时才允许加入。
4. 后续加入的箭头可以挡住先前箭头，从而逐步形成依赖关系。
5. 全部放置完成后，按照“最后加入到最先加入”的顺序消除，天然得到一条完整解法。

如果加入顺序为 `A、B、C`，则构造时 `A` 在空盘可飞出，`B` 在只有 `A` 时可飞出，`C` 在 `A+B` 时可飞出；最终盘面至少可以按 `C、B、A` 清空。依赖边只会从较早加入的箭头指向较晚加入的箭头，因此不会形成环。

构造和正式游戏共用 `Rules.ts` 几何内核，并保留自身出口撞回旧线段的检查。碰撞使用包含宽度的出口走廊和障碍包围盒。现有动画是沿路径抽出，不能改用整根折线刚体平移扫掠，否则会重新引入视觉可退出却被阻挡的旧问题。生成完成后仍使用独立求解器从最终盘面重新求解，防止构造过程、渲染尺寸和运行时判定出现偏差。

为了避免倒序构造只产生单一解法，生成器允许同一深度存在多个互不依赖的箭头，并控制依赖图的宽度、深度和分支数。关卡可以有多条合法消除顺序，但任意一次合法点击都不应把玩家带入无解状态。

### 5.5 配色区域

颜色由距离场和语义区域共同决定，不按箭头编号随机分配：

- 主色：55%～65%，覆盖身体或主体。
- 浅过渡色：20%～30%，形成由外向内的连续层次。
- 蜜桃辅助色：10%～15%，用于耳朵、叶片或局部结构。
- 点缀色：5%～10%，用于尾巴、脸颊、花心等小区域。

整根箭头保持单色。区域边缘通过“多数采样”决定线条颜色，避免一根长线跨区后颜色突兀。

## 6. 综合质量评分与局部重排

通过硬性规则的候选关卡还要进行评分，生成器在限定次数内保留总分最高者，而不是采用第一个可解结果。

| 评分项 | 核心指标 | 低分处理 |
| --- | --- | --- |
| 辨识度 | 关键部件保留率、剪影特征及人工缩略图盲评 | 重生成轮廓或删除无效装饰 |
| 饱满度 | 覆盖率、最大空洞、边缘连续性 | 局部补线、合并或重铺路径 |
| 秩序感 | 路径间距、方向簇、长短线比例 | 重排局部箭头方向与长度 |
| 可读性 | 点击热区重叠、视觉间隙与判定间隙 | 扩大间距或删除冲突箭头 |
| 解谜性 | 初始出口、依赖深度、分支宽度 | 重构局部依赖关系 |
| 新鲜度 | 视觉指纹和近期历史距离 | 更换姿态、部件或 seed |

评分采用硬门槛加加权分数：可解、无重叠、无自身死结和视觉判定一致属于硬门槛，不能用其他高分抵消。局部问题优先只重排对应网格块；连续两次局部修复失败则丢弃整个候选，避免修补累积破坏轮廓。

## 7. 一定有解的验证

每个候选关卡必须依次通过：

1. 路径点位合法且互不重叠。
2. 所有路径为单位正交步进。
3. 没有自身死结。
4. 依赖图无环，并至少有一个初始出口。
5. 独立求解器能移除全部箭头。
6. 从多个随机合法选择序列试玩，不能进入无出口死局。
7. 点击热区不存在大面积歧义。
8. 渲染后的视觉间隙与碰撞间隙一致，不允许“看起来能出去、实际被细线挡住”或相反的情况。

任一检查失败即丢弃候选，不通过修补规则强行放行。

## 8. 不重复策略

### 8.1 精确结构指纹

对归一化后的遮罩、全部路径、箭头方向和颜色区域排序编码，生成 64 位结构哈希。玩家历史中已经存在相同哈希时，增加 `retry` 重新生成。每关保存一个 64 位值，玩一万关也只需要约 80 KB 原始空间。

### 8.2 近似视觉指纹

将剪影归一化为 24×24 位图，生成感知指纹。与最近 200 关的指纹距离过近时拒绝，避免“耳朵只差一格”的小猫被当作新图案。

近似指纹不只比较轮廓，还记录多层视觉结构：

- 主题、姿态、主体数量和部件组合。
- 宽高比、重心、对称度、空洞数量和主要曲线分布。
- 耳朵、尾巴、装饰等语义部件的位置关系。
- 箭头数量、长短线分布、四方向比例和颜色区域占比。
- 依赖图深度、宽度、分支数及其结构摘要。

只有 seed 不同不视为新关卡。同主题再次出现时，主姿态或主要部件关系必须改变，例如“坐着猫”与“趴在云上的猫”可以分别出现，而仅改变耳朵一格或颜色不能通过近似去重。

### 8.3 内容冷却

保存最近类别、母题、姿态和主参数。类别不能连续，母题至少间隔 12 关，姿态至少间隔 6 关。第 25 关虽然颜色回到立春，图案和箭头结构不会回到第 1 关。

## 9. 难度曲线

| 阶段 | 箭头数量 | 初始可退出比例 | 依赖深度 | 主要目的 |
| --- | ---: | ---: | ---: | --- |
| 1～5 关 | 20～35 | 30%～45% | 2～3 | 学会观察方向 |
| 6～20 关 | 35～65 | 20%～35% | 3～5 | 建立消除顺序意识 |
| 21～60 关 | 55～90 | 15%～28% | 4～7 | 开始使用缩放 |
| 61 关以后 | 70～130 | 12%～25% | 5～10 | 循环变化并缓慢升难 |

难度在每 8～10 关后安排一关较轻松的“呼吸关”，避免持续加压。无限阶段只在安全范围内波动，不无限增加箭头数量。

难度主要来自初始出口数量、依赖链深度、分支选择和局部密度。不会使用“视觉上像是能走、实际被极小结构挡住”的方式制造难度；这属于判定可读性缺陷，而不是有效谜题。

### 9.1 当前 demo 难度评估

当前 120 关的批量报告显示：箭头数主要落在 58～66 根，初始可退出比例约 36%～41%，覆盖率约 85%～86%。1～20、21～40、41～60、61～80、81～100、101～120 六个区间的平均箭头数分别约为 66、59、61、62、58、60，生成耗时平均约 187～210 ms。

这说明当前 demo 有可玩的中等密度，但没有形成清晰的成长曲线，也没有实现“低—中—高—中—低—高”的留存节奏。第 1 关的 58～70 根箭头对新用户仍偏密，且后续关卡没有足够明显的呼吸关。当前版本适合验证生成、点击和碰撞规则，不能直接作为 120 关正式难度配置。

正式难度使用独立分数，而不是只看箭头数量：

```text
difficulty =
  0.24 × 依赖深度归一化
+ 0.20 × 初始出口稀缺度
+ 0.18 × 分支选择压力
+ 0.16 × 局部密度
+ 0.12 × 路径长度与转角复杂度
+ 0.10 × 缩放需求
```

视觉相似度、点击热区重叠和碰撞判定不一致属于硬性拒绝项，不计入“难度加分”。

### 9.2 留存节奏

以 6 关为一个短周期，采用“低—中—高—中—低—高”的顺序；每 5 个短周期组成一个 30 关章节。低难度关负责让用户恢复信心、熟悉新主题和减少连续失败，中难度关保持思考感，高难度关提供成就峰值。第 6 关和第 30 关可以作为章节展示关，但不能把复杂度全部堆到箭头数量上。

| 周期位置 | 目标难度 | 箭头数参考 | 初始出口比例 | 依赖深度 | 体验目标 |
| --- | ---: | ---: | ---: | ---: | --- |
| 第 1 关 | 低 | 24～34 | 45%～55% | 2～3 | 无压力上手 |
| 第 2 关 | 中 | 34～48 | 32%～42% | 3～4 | 学会观察顺序 |
| 第 3 关 | 高 | 45～65 | 20%～30% | 5～7 | 形成挑战峰值 |
| 第 4 关 | 中 | 36～52 | 30%～40% | 3～5 | 失败后恢复 |
| 第 5 关 | 低 | 26～40 | 42%～52% | 2～3 | 稳定正反馈 |
| 第 6 关 | 高 | 50～75 | 18%～28% | 5～8 | 周期成就点 |

每 30 关引入一个新的主题组合或构图规则，但首关难度回到低位。玩家连续失败两次时，下一关临时使用同段位的低端参数；这只改变难度配方，不改变关卡编号和视觉去重记录。

## 10. 2,000～3,000 关的长期关卡方案

2,000～3,000 关不应依赖保存 3,000 份 JSON，也不应把所有历史关卡永久放在本地。采用“章节配方 + 可复现 seed + 历史摘要”的方式：玩家只保存已通过关号、当前配方版本、最近 200～500 个视觉指纹和一个章节摘要，关卡本身按需重新生成。

### 10.1 章节分层

| 关卡段 | 内容变化 | 生成重点 |
| --- | --- | --- |
| 1～30 | 六类单体基础姿态 | 教学、低密度、稳定可解 |
| 31～120 | 单体组合和明显姿态变化 | 建立低中高循环 |
| 121～300 | 双部件构图，如猫抱星、狗趴云 | 语义区域和留白控制 |
| 301～600 | 三部件构图、旋转构图和不对称构图 | 结构去重和分支难度 |
| 601～1000 | 主题章节，例如春日、夜色、花园 | 颜色、构图和难度联合排期 |
| 1001～1600 | 多物体组合和特殊路径规则 | 依赖图宽度、局部密度和缩放需求 |
| 1601～2200 | 高阶主题变体、稀有构图 | 稳定性能，避免无意义堆线 |
| 2201～3000 | 章节循环但参数空间扩大 | 全历史摘要、赛季式收藏和挑战关 |

每个章节包含 30～60 个母题配方槽位。配方槽位由主体、姿态、附属部件、相对位置、比例、留白、箭头密度和依赖目标共同组成；颜色只作为节气主题层，不能单独构成新关卡。

### 10.2 长期去重

去重分三层：

1. 精确结构哈希：遮罩、部件关系、路径和依赖图完全相同即拒绝。
2. 近期视觉指纹：最近 200～500 关比较归一化剪影、部件布局和箭头密度，过近即重生成。
3. 章节摘要：每个 30 关保存主题、姿态、部件组合、宽高比、空洞数量、重心和依赖摘要；新章节与最近 10 个章节做抽样比较。

不能承诺数学意义上的永久不重复，但可以保证玩家连续体验中的感知不重复。2,000～3,000 关的历史摘要体积预计在数百 KB 以内，适合本地存档；完整关卡只在当前关和下一关存在于内存。

### 10.3 性能与失败回退

生成器按“主题轮廓—路径候选—真实碰撞—求解—评分”分帧执行。当前报告的 P95 约 334 ms，正式目标是把首关生成隐藏在加载页，并将下一关生成拆成每帧 4～6 ms。超过 30 次候选仍不达标时，按以下顺序回退：减少局部密度、替换附属部件、降低一个难度档、使用同章节的已验证备用配方。

任何回退都必须重新通过求解、视觉相似度和点击判定检查；不能为了生成成功而直接放宽“重复”和“无解”规则。

## 11. 通关后的插画奖励

每个关卡配方同时生成一份轻量终局插画描述，与解谜轮廓共用主题语法和部件参数。最后一根箭头消除后：

1. 剩余线条以低幅度向外舒展或化为彩色光点。
2. 在 300～500 ms 内显露完整的柔和彩色线稿插画。
3. 补上解谜阶段刻意留白的眼睛、嘴巴、铃铛和小花等细节。
4. 显示母题名和当关节气色名，停留后进入下一关。

终局插画不是另一套独立素材，而是同一 `LevelRecipe` 的第二种渲染结果，因此能稳定复现，也不会显著增加包体。它给玩家一个完成图案的视觉奖励，并强化每关主题的记忆点。

## 12. 微信小游戏运行策略

- 将生成核心重构为纯 TypeScript，不依赖 Node.js、文件系统或 Cocos API。
- 进入当前关卡后分帧预生成下一关，每帧预算 4～6 ms。
- 候选最多尝试 30 次；仍失败时降低一个复杂度档位重新生成。
- 内置少量已验证的备用配方，避免极端设备上出现长时间等待。
- 本地保存当前配方、下一关配方、生成器版本和历史指纹。
- 生成器升级后，已开始的关卡继续使用旧配方，新关卡使用新版本。

## 13. 代码改造边界

建议拆分为：

```text
assets/scripts/core/generator/
  Random.ts       可复现随机数与种子派生
  ShapeGrammar.ts 参数化矢量母题
  Rasterizer.ts   轮廓栅格化与距离场
  PathBuilder.ts  正交路径覆盖
  Dependency.ts   可解依赖构造
  Fingerprint.ts  精确与感知去重
  Generator.ts    生成流水线
```

离线工具和微信运行时调用同一个生成核心。`tools/generate.cjs` 只负责批量生成、统计和输出报告，避免出现“工具生成正确、运行时实现不同”的双套逻辑。

## 14. 实施顺序

1. 抽离纯 TypeScript 生成核心和关卡配方。
2. 实现云朵、猫、爱心三个参数化母题，验证变化与可解性。
3. 实现依赖图反向构造、结构指纹和近似指纹。
4. 接入运行时下一关预生成与存档。
5. 扩充到六类至少 24 个母题。
6. 加入综合质量评分、局部重排和通关插画渲染。
7. 批量生成 1,000 关，统计失败率、生成耗时、重复率和难度分布。
8. 人工盲评缩略图辨识度、美观度与近似重复，再调整评分权重。
9. 真机验证后再开放无限关卡入口。

## 15. 首版验收标准

- 连续生成 1,000 关，精确结构重复数为 0。
- 最近 200 关中无明显近似剪影。
- 1,000 关全部通过独立求解器。
- 自身死结、路径重叠、无出口初盘均为 0。
- 视觉可通行与碰撞判定不一致为 0。
- 随机抽取 100 关进行人工盲评，主题辨识率不低于 90%。
- 典型手机单关生成 P95 小于 250 ms，并通过分帧隐藏等待。
- 同一关卡配方在 Node.js 测试和微信小游戏中生成相同结构哈希。

## 16. 第 3 关极限试玩版（2026-09-10，本地待试玩）

第 3 关单独使用 `ExtremeChallenge.ts`，其余关卡沿用普通生成器。当前不是整套无限关卡的难度曲线调整。

- 将爱心轮廓离散成 169 个区域，每个区域用一条覆盖 5×5 格点的随机 Hamilton 路径填满；相邻区域保持标准一格线距，不补一格短箭头。
- 先搜索区域的清除顺序：每个区域的出口射线必须穿过紧邻的前驱区域，且不得穿过任何后继区域。前驱允许是同一行或同一列的远处区域，使解锁顺序能够跨越画面。
- 区域内的头部位于朝出口的边界，最后一段朝外，因此不会被自身挡住。所有区域填满，使每条前驱依赖真实成立。
- 这形成一条完整依赖链：每个未通关状态恰有一个合法选择。独立用 `Rules.ts` 的实际碰撞几何验证，不使用“批量剥层数”代替逐次点击。
- 169 根箭头，每根 24 段；最少 8 次转弯，平均约 12.40 次。测试覆盖 169 个未通关状态和终局，无死锁、交叉或共享格点。
- 新关卡 ID 为 `generated-v2-3-extreme-v1`，拒绝复用旧第 3 关的消除存档。画面副标题为“极限挑战 · 长折线迷宫”。

复核命令：`node tools/test-extreme.cjs`。详细数值见 `tests/extreme-report.json`。覆盖率 100% 指离散后的区域格点占用率，不是像素面积，也不应直接与旧轮廓分辨率的覆盖率对比。

这一版强化的是出口搜索和折线追踪，不能把结构指标等同于已验证的玩家难度分数。分块排布仍有规则感，且只有唯一合法顺序；是否足够有趣、放大后的辨识和真机操作体验，需要试玩评估。

## 17. 全图重连试玩方案（用户确认）

目标：打散固定方块的独立感，保持饱满轮廓、长线跨区域嵌套，以及有解情况下尽量高的出口搜索难度。先只在第 3 关试验，不提交 Git。

短箭头允许用于填补空隙，不设禁止使用或固定比例的规则；它们也必须参与阻挡关系，不能为了凑密度增加大量直接可消除的箭头。长短、跨度和转折服从整体布局与难度。

实现路线：保留可解布局作为种子，在相邻路径之间尝试交换尾部和重连，让路径跨越初始区域；每次修改必须保持不交叉、不共用格点、不自锁、无依赖环。初版保留唯一解锁链，它是全程 1–2 个合法选择要求的更严格特例。不能只检查初盘或批量剥层。

视觉验收：降低仍局限于原始区域的箭头比例、增加跨区连接和长度差异；同时人工查看完整图，不能用转弯次数代替视觉验收。短箭头不是失败，整齐的小方块拼贴和大量独立出口才是失败。

参考：
- [Arrout 实际构造代码](https://github.com/nitzanpap/arrout/blob/main/src/generator/reverse-builder.ts)：候选加入后的依赖环与出口数检查。
- [Arrout 依赖图](https://github.com/nitzanpap/arrout/blob/main/src/generator/dependency-graph.ts)：完整出口射线。
- [Arrow Escape](https://github.com/gtxPrime/arrow-escape)：长线优先与短线补隙思路。
- [Clisby 路径生成器](https://clisby.net/projects/hamiltonian_path/)：局部路径重连启发，不提供箭头可解性保证。

本项目自行实现重连与碰撞验收，不把参考算法的路径连通性当作消除关卡可解性证明。

### 历史交织版结果（已被下方 v4 试验替代）

实现同时包含尾部交换和尾部转接，保留全部占用格点。重连不限制于初始区域；可解性约束从唯一链放宽为两条依赖链的证明条件，全程最多两个合法出口。短线允许存在。

当前第 3 关：169 根，长度 4–67 段，111 根跨越初始区域；独立碰撞图穷举全部 182 个可达状态，无死锁，未通关时有 1–2 个合法出口。ID 为 `generated-v2-3-woven-v2`，副标题“极限挑战 · 交织长线”。统计可由 `node tools/test-extreme.cjs` 重现。

仍待改善：部分外围线条保留方块形，头部位置仍来自初始种子布局。本轮重点是跨区域线身重连，不宣称已消除所有方块感。预览为 `design/level3-woven-preview.png`；以微信真机试玩验证最终可读性。

## 18. 第三关全图轮廓路径试验 v4

取消按块生成箭头，也取消左侧横线、右侧竖线的区域分工。先在心形允许区域建立全图生成树，把树的边界连接为一条贯穿图案的闭合路线，再沿路线切分可变长度的箭头。中间的粗网格只用于构造全局路线，不是单根箭头的边界。

每次从剩余路线中选择可向外退出的头部，再向后截取线身；末端可拐向上下左右任意一个合法方向。后续箭头的出口射线必须遇到上一根被移除的线条，以建立依赖链；只允许一次重新起链，形成至多两条链。完整几何仍交由实际 Rules 碰撞规则验证，测试另行穷举全部可达状态。

本次第三关固定使用筛选后的种子 102，运行时生成几何，并非存储画好的关卡。候选筛选综合轮廓路线覆盖、四向分布和短线比例，不只选择最高覆盖率。后续共享化进展见第 19 节。

- ID：`generated-v2-3-contour-v4`，几何签名 `8d49cf7e`。
- 74 根箭头，1–35 段，平均 2.09 次转弯。
- 右、左、上、下分别 22、11、25、16 根。
- 全部 250 个可达状态已验证，未通关时合法出口始终为 1–2 个。
- 轮廓路线顶点覆盖率 99.11%。此指标不是屏幕着色面积，不能与旧版格点占用率直接比较。
- 保持暖白背景、A 配色和空间分区赋色。

验收脚本：`node tools/test-extreme.cjs`；构建后还需执行 `node tools/test-wechat-build.cjs`，覆盖 Cocos 实际编译产物的生成、碰撞、误点去重、存档和双指缩放。

限制：这版优先解决方向分区与碎块视觉，箭头数量低于旧版；仍有少量短线，长线嵌套处存在重复回折。出口数限制不等于经过玩家验证的难度评分，最终视觉与挑战感由试玩确认。

## 19. 全关卡同步与前 12 关预览

全部关卡通过同一个全图轮廓路径生成入口，删除旧版随机游走填充路径。第三关保留种子 102、几何签名和两条依赖链限制。第一关链数上限 10，第二关 8；其他关卡按 6、8、4、6、8、4 的六关节奏设置上限。链数仅为结构参数，不能当作玩家感受到的难度分数。

共享生成器支持不连通轮廓：生成树扩展为森林，每个连通部分各有连续路线，切分时禁止跨部分串接。普通关卡的粗格需要至少两个原始格点支持，避免边缘向外膨胀掩盖云朵、动物头部的差异。第三关继续使用已确认的取样，保证试玩版本不变。

轮廓排期同时比较原始轮廓和粗格化轮廓；对近 24 关过于相似的候选更换同主题姿态，保留 0.82 原始轮廓阈值，粗格预筛选阈值为 0.80。排期由关号确定，不依赖玩家实际闯关历史；保留最近 96 项缓存，旧关跳转或冷启动的远关跳转会重算必要的排期。数千关冷跳转性能仍需专项优化，不宣称已完成无限关卡质量保证。

每关最多尝试 8 个确定性种子，轮廓路线覆盖率至少 96%，并通过实际碰撞规则的完整解法验证。失败时明确报生成失败，不回退到旧版稀疏排布。第三关只用已验收种子。

README 的预览来自 `tools/preview-levels.cjs` 调用实际生成器，再由 `tools/render-levels.py` 用实际节气配色绘制；不是手工效果图。批量测试覆盖 120 关，检查几何、可解性、四向存在、消除过程中的出口限制、近 24 关填充后轮廓相似度及确定性复现。具体统计见 `tests/generator-report.json`。
