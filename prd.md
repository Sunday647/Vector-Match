# 一箭清空 PRD

版本：v0.4 · 日期：2026-09-09 · 状态：原型开发与玩法验证阶段

## 1. 产品定位与决策摘要

一款竖屏、单指操作的治愈系微信小游戏。彩色箭头线条共同拼成小猫、小狗、云朵等图案，玩家观察方向与阻挡关系，按顺序移除箭头，慢慢解开整幅图案。

核心体验：看见可爱的图案 → 找到出口 → 点击释放线条 → 图案逐渐解开 → 收获一张完整插画。

已确认技术路线：Cocos Creator + TypeScript，只发布独立微信小游戏；日常使用 Cocos 自带浏览器预览，不单独维护 H5 实现。使用 2D 绘制和自定义几何规则；以 3.8 系列为候选，具体版本在技术验证后锁定。详细设计见 [technical-design.md](technical-design.md)。

主要研发难点是图案关卡的可解性、箭头退出规则与触摸准确性；不需要物理引擎。

### 1.1 已明确的用户要求

- 箭头线条使用彩色设计，整体风格治愈。
- 每一关由箭头构成的整体图案不同，可使用小猫、小狗、云朵等主题。
- 误点扣心，被误点的整根箭头线条标红。
- 同一根线条误点两次只扣一次心。
- 产品为独立微信小游戏，使用自己的小游戏 AppID，独立运行与发布，不嵌入其他应用或小程序。
- 仅发布微信小游戏；浏览器作为 Cocos 开发预览入口。
- 箭头沿路径抽出；关卡采用轮廓模板、反向构造、完整验算和人工筛选。
- 视觉参考为用户提供的猫咪截图：密集正交折线填充完整动物剪影，局部配色表现耳朵、脸部和身体；成品颜色更丰富、更明亮。
- 画布必须支持放大缩小，复杂区域允许放大后精确点击，以降低误触风险；这是核心功能。
- 先完成 PRD 和引擎选型、微信接入分析，暂不编写游戏代码。

### 1.2 本稿采用的产品假设

以下是建议默认值，可以在原型评审时调整，并非用户已指定参数。

| 项目 | 默认方案 |
| --- | --- |
| 心数 | 每次挑战 3 心，独立于其他关卡；无体力等待 |
| 重复误点 | 本次挑战内，同一箭头无论误点多少次，累计最多扣 1 心 |
| 标红期限 | 首次误点后持续标红，直至消除；退出续玩保留，重新挑战重置 |
| 时间限制 | 无倒计时，无操作速度奖励 |
| 首版内容 | 先验证 3 关，MVP 扩充为 12 关不同构图 |
| 商业化 | MVP 不接广告、付费和强制分享 |
| 引导与提示 | 首关轻引导；每次挑战可免费提示 1 次 |

## 2. 用户体验与范围

面向喜欢轻度解谜、可爱插画和碎片时间放松的玩家。目标单关体验为 1–3 分钟，入门关可以更短，具体通过试玩调整。

### 2.1 MVP 功能

- 首页：开始/继续、关卡入口、声音和震动设置。
- 游戏页：关卡名称、心数、彩色箭头棋盘、提示、暂停。
- 暂停页：继续、重新挑战、回首页；重新挑战需提示将清空本局进度。
- 成功页：主题插画、温和祝贺语、下一关、回首页。
- 失败页：“先休息一下，再试一次吧”，提供重新挑战和回首页。
- 关卡页：按顺序解锁，已通关可重玩；最后一关显示“本次旅程完成”。
- 本地保存：解锁关卡、设置、当前挑战状态。

暂不包含排行榜、好友对战、每日任务、体力系统、账号云同步、运行时无限生成关卡和关卡编辑器产品界面。

## 3. 核心玩法与判定

### 3.1 箭头与图案

一根箭头是具有唯一 ID 的连续折线，包含尾部、若干转折点和一个箭头头部。点击头部或线身均可操作。首版以水平、垂直线段为逻辑基础，视觉上使用圆角；不加入斜线、分叉、自交、不同线条初始交叠或共用线段。

同一根线首版使用一种颜色，不同线组合成柔和彩色图案。颜色只负责视觉识别，不决定消除关系；不采用同色配对或三消规则。正常色避免接近错误红。

小猫等轮廓必须由箭头本身构成，不能仅靠背景插画替代。背景可有低对比装饰，但不能混淆可点击线条。

### 3.2 移动模型：沿路径抽出

已确认采用“抽线”规则：箭头头部沿最后一段的方向直线向棋盘边界外移动，其余部分依次沿原线条路径跟随，直到尾端离开棋盘。线条不作为刚体整体横移。

因此，新增占用空间只出现在头部前方的退出通道。判断时检查头部沿朝向到棋盘边界的完整通道是否与其他未消除线条相交；不能只检查紧邻一格。自身原路径不构成阻挡。

低保真原型用于验证抽线的视觉和操作体验。如果后续改变移动模型，必须同步修改碰撞判定并重新校验所有关卡。

### 3.3 正确点击

1. 将触点转换到棋盘逻辑坐标，确定唯一命中的箭头。
2. 按当前棋盘状态判断其退出通道。
3. 通道畅通则提交逻辑消除状态和存档，并播放抽出动画；动画结束后移除该箭头的画面，期间锁定棋盘。
4. 该过程不扣心，也不返还此前扣掉的心。
5. 最后一根箭头移除后，仅触发一次通关结算。

首版每次只播放一根箭头的离场动画，期间锁定棋盘点击且不排队，避免并发判定与动画穿插。建议单次动画 250–500ms，较长路径控制在 700ms 内，均为待试玩调节值。

### 3.4 阻挡与几何一致性

- 棋盘边界代表出口，外部装饰和 UI 不参与阻挡。
- 其他箭头的线身和头部都参与阻挡，考虑可见线宽和头部尺寸。
- 逻辑线段与视觉圆角之间预留间距；关卡不得依赖难以分辨的擦边空隙。
- 不把触摸热区用于障碍碰撞，否则会出现“看起来空着却出不去”。
- 关卡校验器与运行时使用同一套判定规则，不能分别近似实现。

### 3.5 误点、红线与扣心

“误点”严格定义为：游戏可操作时，玩家点击了一根当前无法退出的箭头。点击空白、UI、动画期间的棋盘，或系统取消的触摸，不属于误点。

| 操作/状态 | 心数变化 | 线条变化 |
| --- | --- | --- |
| 首次点击受阻箭头 A | -1 | A 的线身和头部永久标红至本局消除 |
| 再次点击仍受阻的 A | 0 | 保持红色，轻微提示受阻 |
| 第三次及以后误点 A | 0 | 与第二次相同 |
| 首次误点另一根 B | -1 | B 标红 |
| A 的障碍移除后点击 A | 0 | A 正常退出，动画仍为红色 |
| 点击空白或被锁定的棋盘 | 0 | 无变化 |
| 退出后继续同一次挑战 | 0 | 恢复心数、红线及消除状态 |
| 重新挑战/重玩关卡 | 恢复 3 心 | 全部恢复原色，清空误点记录 |

去重以“挑战 ID + 箭头 ID”为依据，不以颜色、折线段或点击位置判断。第一次扣心时，同时记录该箭头已扣心并标红；这些状态作为一次完整变更保存。

例：初始 3 心，误点 A 后 2 心，再误点 A 三次仍为 2 心，误点 B 后 1 心，首次误点 C 后 0 心并失败。

扣至 0 心：先呈现本次变红反馈，再显示失败页，棋盘立即停止接受操作。只有重新挑战才重置该局状态。

允许玩家反复尝试红线，这是规则带来的宽容性设计，不追加隐藏惩罚。红色表示“本局曾误点”，不代表“现在仍受阻”；首次出现红线时提示这一区别。

### 3.6 触摸容错与提示

- 建议线宽为设计坐标 6–9，触摸热区宽度约 24–32 个逻辑像素，最终按真机调节。
- 多根线热区重叠时选择离触点最近的可见线段/箭头头部；距离过近难以区分时不执行、不扣心，给轻微定位反馈。
- 不因为一根线可消除就优先选中它；命中只由触点决定。
- 用单次抬手确认点击；滑动、取消、多指输入不产生误扣。
- 提示高亮一根当前可退出的箭头，不自动消除、不改写红线颜色；用外圈或光晕表示。
- 正常关卡始终应有可移动箭头。若运行时发现无解，视为内容异常，提供重开并记录诊断，不扣玩家心数。

## 4. 美术、声音与界面

采用暖白或极浅蓝背景、明亮清晰的彩色线条。图案内部以密集正交折线填充完整动物剪影，保留折线结构，仅轻微柔化转角，图案外围留白。参考图通过局部颜色表现耳朵、面部、身体与尾巴；正式设计保留这种分区构图，提高色彩丰富度，减少大片黑褐线条。

建议初始调色板：天空蓝 #249EF0、青绿 #14B8A6、草绿 #70B83D、金黄 #D9A515、橙色 #F39A32、紫色 #9163E8、粉色 #DC73BE。均为设计候选，需检查浅背景上的细线清晰度。按特征区域分配主色和辅助色，相邻线条保持区分，不对所有箭头完全随机上色；错误红单独保留。

错误红建议偏柔和珊瑚红，但须与所有正常色明显区分。错误反馈同时使用轻微摆动和短提示，不只依靠颜色，不使用强烈闪屏和刺耳音效。震动可关闭。

移除一根箭头配轻柔音符，连续操作可有轻微音阶变化。背景音乐与音效可分别关闭；切后台暂停，回前台按用户设置恢复。

游戏页顶部展示关卡名与 3 颗心，中部为图案，下方提供缩放和游戏操作。适配刘海、安全区、微信右上角胶囊，不在易误触处布置操作。为支持参考图的密集布局，设计增加双指缩放、放大后拖动、加减缩放按钮及恢复全图；H5 同时支持鼠标拖动和滚轮缩放。手势期间不触发箭头点击或扣心，缩放只改变视图、不改变阻挡判定。具体范围通过原型验证。

清空后以淡入方式展示与本关轮廓一致的主题插画，作为情绪奖励；不在点击过程中用强烈特效遮住棋盘。

### 4.1 画布缩放与防误触

- 默认完整展示图案；手机支持双指捏合缩放和放大后单指拖动，提供加减按钮与“查看全图”。H5 支持滚轮缩放及鼠标拖动。
- 双指缩放围绕手势中心，滚轮缩放围绕指针，按钮缩放围绕棋盘视口中心，尽量保持正在观察的区域位置稳定。
- 缩放只作用于棋盘，关卡名、心数和操作按钮保持固定尺寸。平移范围受约束，不能把整幅图案拖出可见区域。
- 点选在抬手时确认。出现第二个触点或移动超过拖动阈值后，本轮手势取消点击资格，直到所有触点离开；缩放结束剩余一指时也不能触发箭头点击。
- 放大后，线条及间距随画布增大。触摸容错按屏幕尺寸计算后换算到棋盘坐标，避免热区随缩放无限增大、抵消精确点选收益。
- 多条线命中仍难以区分时不执行、不扣心，可轻提示“放大后更容易点选”。不强制弹窗或自动缩放。
- 缩放上下限通过最密集关卡真机验证确定，最大倍率必须让密集区域的相邻箭头能够清楚区分和单独点选。
- 验收包括：缩放与拖动全过程不扣心；缩放后命中正确；查看全图不改变进度；所有区域放大后可通过拖动到达。

## 5. 关卡设计与可解性

### 5.1 首版关卡规划

以下线数为早期玩法验证预算，不是最终视觉密度上限，也不是已生成或验证的数据。用户已提供密集填充参考，正式猫狗关需按轮廓辨识度与缩放操作重新确定线数，并增加一关接近参考密度的压力样例。入门关仍保持低密度。

| 关卡 | 整体构图 | 建议线数 | 设计重点 |
| --- | --- | --- | --- |
| 1 | 单朵云 | 6–8 | 学会方向与退出 |
| 2 | 猫咪头像 | 10–14 | 引入简单阻挡与心数说明 |
| 3 | 小狗头像 | 14–18 | 学会从外围逐步解开 |
| 4 | 爱心气球 | 18–22 | 两个局部区域 |
| 5 | 兔子侧脸 | 20–24 | 简单依赖链 |
| 6 | 蘑菇小屋 | 22–28 | 长短线组合 |
| 7 | 鲸鱼喷水 | 24–30 | 多个可选出口 |
| 8 | 花束 | 26–32 | 局部观察 |
| 9 | 月亮与星星 | 28–34 | 多区域构图 |
| 10 | 小狗蜷睡 | 30–36 | 较长依赖链 |
| 11 | 小猫伸懒腰 | 32–40 | 综合观察 |
| 12 | 云上热气球 | 36–44 | 首章收尾 |

不同关卡不能仅换颜色、旋转或镜像同一布局；同一主题复用时需改变轮廓、姿态与内部箭头布局。早期至少保留多个明显出口，后期通过依赖关系增加难度，不通过缩小热区或降低对比度增加难度。

### 5.2 制作流程

1. 设计可辨识的轮廓草图，选取隐藏逻辑网格。
2. 将轮廓转为可放置区域和关键特征约束，在隐藏网格上反向构造箭头：每次新加入的箭头必须能在当前棋盘直接退出，允许它阻挡之前加入的箭头。按加入顺序的逆序即可得到完整解。
3. 校验路径合法、无交叠、线间距和点击密度达标。
4. 使用与运行时相同的几何模型，计算每根线的阻挡集合。
5. 模拟逐根移除，生成至少一条完整解序列；不能全部清除的候选关卡拒绝发布。
6. 人工检查图案辨识度、操作准确性、解谜节奏，再由真机试玩复核。

静态规则下，移除线条只会减少障碍，不会产生新障碍，因此选择任意当前合法箭头不应让原本可解的关卡死锁。可将阻挡表示为依赖图，用无环性和完整消除模拟交叉验证。后续若加入移动障碍或旋转，必须重新评估这一性质。

先用人工设计轮廓和关键线条、程序生成候选、离线校验及人工挑选完成 3 关，再扩大到 12 关。初始关键线条也必须可解，并通过后续完整校验。图片直接转线条不保证可解；运行时随机拼图也不作为 MVP 方案。

反向构造保证符合条件的布局可解，不保证任意轮廓都能以指定密度填满。生成遇到死胡同时回退、换路径或调整密度，达到尝试上限仍失败的候选不发布。以初始可移动数量、依赖链深度、每步选择数、图案辨识度和触摸间距筛选质量。

## 6. 状态、存档与验收

### 6.1 概念数据模型

| 对象 | 主要字段 | 用途 |
| --- | --- | --- |
| 关卡 | levelId、version、theme、boardBounds、arrows | 定义布局与资源 |
| 箭头 | arrowId、pathPoints、direction、baseColor | 描述固定路径，方向与末段一致 |
| 挑战 | attemptId、levelId、levelVersion、hearts、removedIds、penalizedIds、hintUsed、status | 恢复本局，防重复扣心 |
| 玩家本地进度 | unlockedLevel、completedLevels、settings | 记录解锁与设置 |

红色显示由 penalizedIds 推导，不独立维护一份容易失配的颜色状态。存档在每次有效操作后保存，退后台再补存。动画中断恢复到确定状态：合法消除已提交则恢复为已消除，不重复计数或重播扣心。

存档版本与关卡版本不匹配时，保留解锁进度并提示本关重新开始。写入失败给予轻提示，不在当前会话重复扣心。MVP 本地存档不承诺跨设备同步。

### 6.2 核心验收用例

| 用例 | 预期 |
| --- | --- |
| 可退出线身、箭头头部各点击一次 | 均可准确命中，完整退出，不扣心 |
| 前方远处有线、近处为空 | 判为受阻，不穿透 |
| 连续误点同一根线不同位置 | 总计只扣 1 心，整根线及头部标红 |
| 连续误点三根不同线 | 3→2→1→0，仅弹一次失败页 |
| 红线障碍清除后再次点击 | 正常退出，不返心、不额外扣心 |
| 标红后退出并继续 | 心数、红线、去重记录一致 |
| 重新挑战 | 3 心、原色、全部箭头恢复、提示次数重置 |
| 空白、模糊热区、拖动、动画中连点 | 不误扣、不触发多次结算 |
| 最后一根箭头消失 | 仅结算一次并解锁下一关 |
| 每个正式关卡 | 完整解序列通过校验，图案人工可辨认 |
| 消除动画期间切后台/重启 | 恢复状态一致，无幽灵箭头 |
| 双指缩放、拖动、滚轮及按钮缩放 | 不误扣心，变换后命中正确，阻挡结果不变 |

### 6.3 性能与体验目标

这些是待测目标，不是当前已实现结果。正式测试需记录设备、系统、微信版本与网络条件。

- 在选定的一台中低端 Android 和一台较旧 iPhone 上验证；覆盖窄屏和长屏。
- 首次进入可操作关卡目标 ≤5 秒，测试条件为稳定 Wi-Fi；同时记录弱网表现。
- 棋盘交互争取 60 FPS，中低端设备稳定不低于 30 FPS。
- 点击后首个视觉反馈目标 ≤100ms；连续游玩 20 关无持续内存增长。
- 首关试玩关注是否能理解箭头方向，前 3 关关注误触是否被误认为谜题失败。

后续灰度可统计开始/通关/失败/重开、首次扣心、重复误点、提示使用和单关时长。重复误点单独计数，不能把它当作多次扣心；首版先使用本地诊断，不因此引入账号系统。

## 7. 游戏引擎选型

以下比较是基于本项目需求的工程判断，不是跨引擎性能实测排名。

| 方案 | 适配本项目的优势 | 主要代价 | 建议 |
| --- | --- | --- | --- |
| Cocos Creator + TypeScript | 2D 场景/UI、动画、资源管理完整；官方有微信小游戏构建流程 | 需熟悉编辑器，控制模块与资源体积；线条命中仍需自己实现 | 首选 |
| LayaAir + TypeScript | 官方支持微信小游戏，适合 2D 与 Web 工作流 | 同样需掌握工具链、自定义判定和内容流程 | 团队已有经验时可作为首选替代 |
| Phaser | 适合浏览器 2D 原型，代码开发直观 | 本次查到的微信官方站内案例为历史 Phaser CE 方案，不能据此认定当前版本可直接发布 | Web 优先且有适配经验时再选 |
| 原生小游戏 Canvas 2D + TypeScript | 绘制简单，依赖可少，规则完全可控 | 自建 UI、布局、动画、音频、资源和生命周期管理 | 极小规模验证可行，长期内容迭代成本需评估 |

Cocos 的推荐依据是项目需要同时做好图案线条、界面、反馈和微信交付，而非复杂图形能力。其 Graphics 提供线宽、描边色、端点和路径绘制能力，可用于彩色箭头；命中、阻挡和去重由游戏逻辑独立负责。[Cocos Graphics 文档](https://docs.cocos.com/creator/3.8/manual/zh/ui-system/components/editor/graphics.html)

LayaAir 官方文档列出微信小游戏支持，因此不是能力不足；若实际开发团队已熟悉它，切换到 Cocos 未必有收益。[LayaAir 小游戏文档](https://www.layaair.com/3.x/doc/released/minigame/)

Phaser 本次可核实的微信教程发布于 2018 年，针对 Phaser CE；这不足以支持当前版本开箱即用的结论。[Phaser 历史教程](https://phaser.io/news/2018/01/running-phaser-on-wechat)

### 7.1 在 Cocos 中如何使用

- 棋盘视图：初期每根线独立绘制，变红时只重绘该线，空闲时不逐帧重建路径。
- 动画：根据原路径与头部延伸路径计算抽出效果；性能测试后再考虑合批或自定义网格。
- 规则层：纯 TypeScript 管理命中后的操作判定、阻挡、心数、状态机，不依赖渲染帧率。
- 内容层：读取关卡数据与主题资源，不为每关复制一套游戏代码。
- 平台层：统一封装本地存储、前后台事件、声音与震动，方便浏览器预览和微信真机差异处理。
- 工具层：离线关卡校验复用规则层，输出解序列与错误报告。

不启用 3D 和物理模块；实际可裁剪模块以所锁定版本的构建配置为准。MVP 无需后端，首批关卡尽量随包，扩展内容再评估资源分包与远程资源。

## 8. 独立微信小游戏的构建与发布

### 8.0 单工程开发工作流

仅维护 Cocos Creator 源工程，使用编辑器浏览器预览调试玩法、画面和交互，使用微信开发者工具与真机验证平台行为，最终构建独立微信小游戏。不开发另一套 Canvas H5 实现，也不将独立 H5 发布作为交付要求。

### 8.1 已确认的发布目标

产品确定为独立微信小游戏，使用自己的小游戏 AppID 和工程，独立完成启动、游戏界面、关卡、存档与发布。用户直接进入小游戏即可游玩，无需其他应用承载。

微信官方示例使用 game.js 入口和小游戏适配器，可作为理解小游戏工程结构的参考。[微信官方小游戏示例](https://github.com/wechat-miniprogram/miniprogram-game-quickstart)

### 8.2 推荐接入步骤

1. 准备微信小游戏项目及 AppID，安装微信开发者工具和选定的 Cocos Creator。
2. 创建 Cocos 2D 项目，以 TypeScript 编写逻辑并配置竖屏场景。
3. 在 Cocos 中实现棋盘、界面、主题资源与平台适配；浏览器预览用于快速验证。
4. 在构建发布面板选择“微信小游戏”，填写 AppID、方向和需要发布的场景，执行构建。
5. 将生成目录导入微信开发者工具，或从 Cocos 启动工具。输出默认位于 build/wechatgame，实际依构建任务名变化，包含 game.json、project.config.json 等配置。
6. 完成开发者工具调试与 Android/iOS 真机预览，验证触摸、音频、前后台及存档。
7. 真机通过后，再走上传、体验版与提审发布流程；具体提交要求按届时后台执行。

Cocos 官方发布文档确认上述引擎构建与工具导入方式。引擎运行时及平台适配随构建输出，形成独立小游戏工程；正式维护 Cocos 源工程，避免直接修改可被覆盖的生成目录。[Cocos 微信小游戏发布文档](https://docs.cocos.com/creator/3.8/manual/zh/editor/publish/publish-wechatgame.html)

### 8.3 引擎插件、包体与平台边界

“微信小游戏引擎插件”是构建选项中的引擎分离能力。首个验证包先采用普通构建；包体实测后，再核实所选版本是否适合启用插件。

上线前以微信最新文档和开发者工具检查结果确认主包、分包、资源下载与插件限制，本稿不把历史容量数字写为交付约束。远程资源方案如启用，需单独设计失败重试、缓存版本与加载反馈。

微信官方开发指南在本次检索中未能直接读取；已核实的构建细节以 Cocos 官方文档和微信官方示例为依据。平台配额和上线材料，实施前仍需通过微信官方文档或后台核验。

## 9. 实施顺序与下一阶段决策

### 阶段一：产品与视觉原型

制作云朵、猫、狗三个低保真关卡，确认抽线运动是否符合预期、轮廓是否可辨认、红线含义是否清楚。此阶段尚不需要批量生产关卡。

### 阶段二：最小技术验证

后续进入开发时，验证绘制、触摸命中、阻挡、去重扣心、抽线动画、缩放拖动、H5 本地预览和微信真机运行。除早期低密度关卡外，增加接近用户参考图密度的样例，实际线数由布局确定并记录。通过后锁定引擎补丁版本与逻辑坐标方案。

### 阶段三：MVP

扩充 12 关，完成界面、存档、设置、音效、提示和关卡校验，按第 6 节验收。出现不可解或误触问题时优先修关卡与交互，不通过放宽判定掩盖问题。

### 当前需在原型中重点确认的三项

1. 箭头采用“沿路径抽出”的运动效果是否符合预想。
2. 3 心、重复误点永久去重、红色保持到消除，是否达到预期宽容度。
3. 主题辨识度与线条密度是否能兼顾，决定正式关卡数量和制作成本。

当前交付为 PRD 与技术方案文档；未创建引擎工程、未编写游戏代码、未进行性能实测。
